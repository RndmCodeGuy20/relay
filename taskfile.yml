version: '3'

dotenv: [".env", ".env.local", ".env.test"]

includes:
  build: ./taskfiles/build.yml
  dev: ./taskfiles/dev.yml
  db: ./taskfiles/db.yml
  lint: ./taskfiles/lint.yml
  test: ./taskfiles/test.yml

vars:
  APP_NAME: relay
  BUILD_DIR: build
  MAIN_PATH: cmd/main.go

tasks:
  default:
    cmds:
      - task: dev:run

  ci:
    desc: Run full CI pipeline
    cmds:
      - task: lint:all
      - task: test:all
      - task: build:prod